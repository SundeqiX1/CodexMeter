# Local build artifacts

Run one of these commands from `apps/desktop-tauri`:

```bash
npm run package:mac
npm run package:windows
```

The resulting application, installer, and checksum file are copied into this directory. Generated binaries are intentionally ignored by Git.
